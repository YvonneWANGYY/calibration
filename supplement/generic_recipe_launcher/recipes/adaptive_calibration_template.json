{
  "name": "adaptive_calibration_template",
  "description": "Generic adaptive calibration recipe template for recipe-driven parameter-space exploration.",
  "python_exec": "python",
  "engine": "scripts/<your_adaptive_calibration_engine>.py",
  "user_edit_notes": {
    "scope": "This is a generic public template, not a ready-to-run local configuration.",
    "edit_before_running": [
      "Replace the science target and loss description with your calibration objective.",
      "Replace the parameter_space entries with your simulator parameters and bounds.",
      "Replace the initial_design and simulator wrapper fields with your local artifacts.",
      "Keep scheduler submission outside this JSON; write your own wrapper around the printed command for your local batch system or local execution."
    ]
  },
  "science_target": {
    "target_file": "targets/<your_target>.json",
    "observable_keys": [
      "<observable_1>",
      "<observable_2>"
    ],
    "loss_kind": "<your_loss_kind>"
  },
  "parameter_space": [
    {
      "name": "<parameter_1>",
      "min": 0.0,
      "max": 1.0,
      "scale": "linear"
    },
    {
      "name": "<parameter_2>",
      "min": 1.0,
      "max": 10.0,
      "scale": "log"
    }
  ],
  "initial_design": {
    "summary": "analysis_outputs/<your_initial_design_summary>.csv",
    "label": "<your_initial_design_label>"
  },
  "simulator": {
    "wrapper": "scripts/<your_simulator_wrapper>.py",
    "template": "configs/<your_simulator_template>.yaml",
    "command_template": "python scripts/<your_simulator_wrapper>.py --params {theta_json} --output {run_dir}"
  },
  "proposal_model": {
    "type": "crqsf",
    "model_root": "models/<your_proposal_model_root>",
    "acquisition": "<your_acquisition_rule>",
    "candidate_pool_size": 64,
    "seed": 1
  },
  "run_control": {
    "start_iter": 0,
    "max_iters": 20,
    "num_samples_per_iter": 1,
    "random_seed": 123
  },
  "outputs": {
    "root": "runs/<your_output_root>",
    "label": "<your_run_label>",
    "manifest": "launch_manifest.json"
  },
  "scheduler": {
    "policy": "external",
    "note": "This recipe prints a calibration command; submit it with your own local scheduler or shell wrapper."
  }
}
